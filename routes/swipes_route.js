var express = require('express');
var router = express.Router();

/* POST rating info. */
router.post('/swipe/:id', function(req, res, next) {
  let swipe_info = req.body
});

module.exports = router;